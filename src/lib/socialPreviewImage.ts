/**
 * `public/preview.jpeg` — dimensions from the asset (sips); used for OG / Twitter metadata.
 * Path has no query string so X’s composer reliably fetches the same URL as in meta tags.
 */
export const SOCIAL_PREVIEW_PATH = "/preview.jpeg";
export const SOCIAL_PREVIEW_WIDTH = 1600;
export const SOCIAL_PREVIEW_HEIGHT = 852;
